# PDF盖章小工具需求说明书

## 项目概况
*   **项目名称**: 本地PDF盖章小工具
*   **核心目标**: 实现本地PDF文档的快速盖章功能，支持签章管理与批量操作
*   **涉及端点**: 本地桌面应用

## 功能需求

### 1. PDF文档处理
- 支持PDF文件的加载和预览
- 支持PDF文档的页面导航（上一页、下一页）

### 2. 签章管理
- 支持上传图片作为签章（支持PNG、JPG等格式）
- 支持查看已保存的签章列表
- 支持删除不需要的签章
- 支持设置默认签章

### 3. 精确盖章
- 支持在PDF页面上选择签章位置
- 支持选择盖章页面（当前页、全部页、自定义范围）
- 支持签章的放大缩小功能
- 支持签章的精确定位和调整
- 支持调整签章大小与透明度

### 4. 本地保存
- 支持将盖章后的PDF保存到本地（PNG、JPG、PDF三种格式）
- 保留原始文件，生成新的盖章文件

### 5. 数据库存储
- 使用IndexedDB数据库保存签章信息
- 支持保存已盖章的PDF文件到数据库

## 业务需求规格说明书

| 端点             | 模块              | 需求 ID    | 需求名称 (Label) | 业务逻辑 (Business Logic)                                            | 异常/边界处理 (Exception Handling)       |
| :--------------- | :---------------- | :--------- | :--------------- | :------------------------------------------------------------------- | :--------------------------------------- |
| 本地桌面应用     | PDF处理模块       | PDF-001    | PDF文档上传     | 1. 支持选择本地PDF文件上传到系统<br>2. 实时预览上传的PDF文档内容     | 1. 上传文件非PDF格式时提示错误<br>2. 文件过大时显示加载状态         |
| 本地桌面应用     | PDF处理模块       | PDF-002    | 签章添加功能     | 1. 从签章库选择签章图片<br>2. 在PDF预览界面拖拽定位签章位置<br>3. 支持调整签章大小与透明度 | 1. 未选择签章时提示先添加签章<br>2. 签章超出页面边界时自动调整       |
| 本地桌面应用     | PDF处理模块       | PDF-003    | 盖章页面选择     | 1. 支持选择当前页面盖章<br>2. 支持选择全部页面批量盖章<br>3. 支持自定义页面范围盖章 | 1. 页面范围输入错误时提示修正<br>2. 无权限编辑PDF时提示错误         |
| 本地桌面应用     | 签章管理模块     | SIG-001    | 签章上传功能     | 1. 支持上传图片格式的签章文件<br>2. 保存签章到本地签章库             | 1. 图片格式不支持时提示错误<br>2. 图片过大时提示压缩建议           |
| 本地桌面应用     | 签章管理模块     | SIG-002    | 签章库管理     | 1. 显示已保存的所有签章列表<br>2. 支持删除不需要的签章<br>3. 支持设置默认签章 | 1. 签章库为空时提示添加签章<br>2. 删除签章时提供确认提示             |
| 本地桌面应用     | PDF处理模块       | PDF-004    | 盖章结果导出     | 1. 支持将盖章后的PDF导出到本地（PNG、JPG、PDF三种格式）<br>2. 保留原始文件，生成新的盖章文件 | 1. 导出路径不存在时提示选择有效路径<br>2. 导出失败时显示错误信息     |
| 本地桌面应用     | 数据库模块       | DB-001     | 数据持久化     | 1. 使用IndexedDB数据库保存签章信息<br>2. 支持保存已盖章的PDF文件到数据库 | 1. 数据库操作失败时提示错误<br>2. 存储空间不足时提示清理数据         |

## 技术实现

### 1. 前端技术栈
- HTML5 + CSS3 + JavaScript（原生）
- PDF.js 用于PDF文档的加载和渲染

### 2. 数据存储
- 使用IndexedDB数据库存储签章信息和已盖章的PDF文件
- 文件处理在客户端完成，确保安全性

### 3. 主要功能模块
- PDF加载与预览模块
- 签章管理模块
- 盖章操作模块（支持拖拽、缩放、定位）
- 文件保存模块（支持多种格式）
- 数据库管理模块

## 工程化执行映射表

| 目标文件名 (File Path) | 页面标题 (Title) | 归属模块 (Module ID) | 视图类型 (Type) | 核心指令与业务定义 (Core Logic - 必须严格执行) | 开发策略 (Strategy) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `pages/index.html` | 首页 | `common` | `web` | **[必须包含]** 工具介绍/功能入口/文件上传区域 | **[展示型]** |
| `pages/pdf-editor.html` | PDF编辑器 | `pdf` | `web` | **[必须包含]** PDF预览组件/签章拖拽功能/页面选择器/导出按钮 | **[复杂交互型]** |
| `pages/signature-manager.html` | 签章管理 | `signature` | `web` | **[必须包含]** 签章列表/上传按钮/删除功能/默认设置 | **[列表型]** |
| `js/pdf-service.js` | PDF处理服务 | `pdf` | `web` | **[必须包含]** PDF加载/签章添加/页面处理/导出功能 | **[功能型]** |
| `js/signature-service.js` | 签章管理服务 | `signature` | `web` | **[必须包含]** 签章存储/读取/删除/默认设置功能 | **[功能型]** |
| `js/database-service.js` | 数据库服务 | `database` | `web` | **[必须包含]** IndexedDB初始化/数据存储/读取/删除功能 | **[功能型]** |
| `styles/main.css` | 主样式文件 | `common` | `web` | **[必须包含]** 响应式布局/组件样式/交互效果 | **[样式型]** |

*   **Type 说明**: 必须严格为 `web`。
*   **Dev Strategy 参考**: `[展示型]`, `[表单型]`, `[列表型]`, `[详情型]`, `[图表型]`, `[复杂交互型]`, `[功能型]`, `[样式型]`。

## 项目结构

```
小工具/
├── pages/
│   ├── index.html          # 首页，PDF文件上传
│   ├── pdf-editor.html     # PDF编辑器，盖章功能
│   └── signature-manager.html  # 签章管理页
├── js/
│   ├── signature-service.js    # 签章服务
│   ├── pdf-service.js          # PDF服务
│   └── database-service.js     # 数据库服务
└── styles/
    └── main.css            # 主样式文件
```

## 使用说明

1. 从首页上传PDF文件
2. 在PDF编辑器页面选择需要使用的签章
3. 选择盖章页面（当前页、全部页或自定义范围）
4. 在PDF预览区域点击选择盖章位置
5. 调整签章大小和位置（可选）
6. 选择导出格式（PNG、JPG或PDF）
7. 点击保存按钮完成操作

## 特点

- **本地处理**：所有操作都在本地完成，确保文档安全性
- **多种格式支持**：支持多种图片格式作为签章，支持多种格式导出
- **精确控制**：支持签章的精确定位和大小调整
- **数据库存储**：使用IndexedDB存储签章和已盖章的PDF文件
- **简单易用**：直观的用户界面，操作流程简单清晰